using UnityEngine;

public class artifactStarter : MonoBehaviour
{
    public GameObject playerContrtoller;
    
    
    // gonna take information based on which sprite is chosen, and then execture based off that when its assigned in a slot.
    void Start()
    {
      
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void assignedArtifact(string nameOfArtifact) 
    {
       /*Debug.Log("New artifact: " + nameOfArtifact + " on: " + gameObject.name);
       if(nameOfArtifact == "Test1")
        {
            //playerContrtoller.GetComponent<FirstPersonController>().walkSpeed = 40;
            Debug.Log("Change the sword hit amt");
        }
       else
        {
            Debug.Log("Dk what artifact");
        }*/
    }  
}
